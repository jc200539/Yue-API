<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yue.project.mapper.UserInterfaceInfoMapper">

    <resultMap id="BaseResultMap" type="com.yue.apicommon.model.entity.UserInterfaceInfo">
            <id property="id" column="id" />
            <result property="user_id" column="user_id" />
            <result property="interface_info_id" column="interface_info_id" />
            <result property="total_num" column="total_num" />
            <result property="left_num" column="left_num" />
            <result property="status" column="status" />
            <result property="create_time" column="create_time" />
            <result property="update_time" column="update_time" />
            <result property="is_delete" column="is_delete" />
    </resultMap>

    <sql id="Base_Column_List">
        id,user_id,interface_info_id,total_num,left_num,status,
        create_time,update_time,is_delete
    </sql>

    <update id="updateLeftNumByIncrement">
        update `user_interface_info`
        set left_num = left_num + #{increment}
        where id = #{id}
          and left_num = #{leftNum}
    </update>

    <select id="listTopInvokeInterfaceInfo" resultType="com.yue.apicommon.model.entity.UserInterfaceInfo">
        select interface_info_id, sum(total_num) as totalNum
        from `user_interface_info`
        group by interface_info_id
        order by total_num desc limit #{limit};
    </select>
</mapper>
